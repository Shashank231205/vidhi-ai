[project]
name = "vidhi-ai"
version = "0.1.0"
description = "Unified AI legal platform for Indian law"
requires-python = ">=3.12"

dependencies = [
    "fastapi>=0.118",
    "uvicorn[standard]>=0.37",
    "pydantic>=2.11",
    "pydantic-settings>=2.7",
    "httpx>=0.28",
    "sse-starlette>=2.2",
    # data layer
    "asyncpg>=0.30",
    "sqlalchemy[asyncio]>=2.0.36",
    "pgvector>=0.3.6",
    "alembic>=1.14",
    # agents
    "langgraph>=0.3",
    "langchain-core>=0.3",
    # cache
    "upstash-redis>=1.2",
    # ingestion
    "pypdf>=5.1",
    "python-multipart>=0.0.20",
    "structlog>=25.1",
    "tenacity>=9.0",
]

[project.optional-dependencies]
# Only for EMBEDDING_BACKEND=local. It pulls torch, which is ~2.5GB installed
# and drags a 2.2GB model download on top — the deployed image is ~9GB with it
# and ~400MB without. Both backends call the same BGE-M3 weights and produce
# identical 1024-dim vectors, so a corpus embedded by one is valid for the
# other; the deployment uses the remote backend and skips this entirely.
local-embeddings = [
    "sentence-transformers>=3.3",
]
dev = [
    "pytest>=8.3",
    "pytest-asyncio>=0.25",
    "pytest-cov>=6.0",
    "ruff>=0.9",
    "mypy>=1.14",
    "respx>=0.22",
]
ml = [
    "accelerate>=1.1",
    "pandas>=2.2",
    "pyarrow>=18",
    "scikit-learn>=1.6",
]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "N", "UP", "B", "A", "C4", "SIM", "RUF", "ASYNC"]
ignore = ["A003"]

[tool.ruff.lint.per-file-ignores]
# Unicode look-alikes are the subject under test, not a mistake.
"tests/test_verifier.py" = ["RUF001"]

[tool.mypy]
python_version = "3.12"
strict = true
warn_return_any = true
disallow_untyped_defs = true
plugins = ["pydantic.mypy"]

[[tool.mypy.overrides]]
module = ["pgvector.*", "upstash_redis.*", "langgraph.*"]
ignore_missing_imports = true

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
markers = [
    "integration: exercises a real hosted service; skipped without credentials",
]
