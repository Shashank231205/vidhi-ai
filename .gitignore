# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Environment — never commit real credentials
.env
.env.*
!.env.example

# Node / Next.js
node_modules/
.next/
out/
.vercel/

# Trained model weights. Reproducible from ml/*/train.py, and far too large
# for git — metrics.json is committed so the numbers stay reviewable.
ml/*/model/**
!ml/*/model/metrics.json
ml/*/checkpoints/
*.safetensors
*.bin

# Downloaded corpus. Statute PDFs and judgment dumps land here and run to
# hundreds of MB; they are re-fetchable from scripts/ingest_*.py, and the
# ingested text lives in Postgres rather than in git.
data/

# Editor / OS
.DS_Store
.idea/
.vscode/*
!.vscode/settings.json
